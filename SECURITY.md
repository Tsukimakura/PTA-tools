# 安全策略

PTA-Tools 会处理账号密码、会话 Cookie、钉钉 Webhook 和答题内容。请谨慎保管这些信息，并负责任地披露安全问题。

## 支持范围

当前仅维护 `main` 分支的最新版本。旧提交、个人分支和第三方修改版本不保证获得安全修复。

PTA 平台本身、钉钉服务及其基础设施的问题不属于本项目的安全修复范围，应报告给对应平台。

## 报告漏洞

请不要为未修复漏洞创建公开 Issue，也不要在公开日志、截图或复现仓库中包含真实凭据。

请通过邮件联系：

- [chenlingshi@zju.edu.cn](mailto:chenlingshi@zju.edu.cn)

报告中建议包含：

- 漏洞类型及可能影响；
- 受影响版本或提交；
- 最小化复现步骤或概念验证；
- 建议的缓解或修复方案；
- 是否已经向其他人公开。

请使用测试账号和无效化后的凭据进行复现。维护者会尽力在 7 天内确认收到报告，并在验证后协调修复及披露时间。

## 优先关注的问题

- Cookie、密码、Webhook 或个人信息泄露；
- 任意文件读取、写入或路径穿越；
- 命令注入或不安全的子进程调用；
- 未经用户确认开始考试或提交答案；
- 错误账号、题目集或提交 ID 导致的数据完整性问题；
- 钉钉消息被注入或发送到非预期目标；
- 依赖链中的可利用漏洞。

以下内容通常不作为本项目漏洞处理：

- 仅影响已停止支持版本的问题；
- PTA 或钉钉服务自身的可用性问题；
- 需要用户主动公开自己凭据才能触发的问题；
- 不提供实际影响或复现依据的自动扫描结果；
- 对真实平台进行压力测试或高频扫描。

## 用户安全建议

- 优先通过环境变量提供账号密码；
- 不要提交或分享 `config.json`、`.env`、`pta_status.json` 和导出文件；
- 定期更新 Cookie 和钉钉机器人令牌；
- 怀疑泄露后立即退出 PTA 会话并重置机器人 Webhook；
- 只从可信来源安装依赖，并定期运行 `npm audit`；
- 在共享计算机上避免保存长期有效的 Cookie。

## 披露原则

在修复发布且用户有合理升级时间之前，请保持漏洞细节私密。修复完成后，可在征得报告者同意的情况下在变更记录中致谢，但不会公开敏感复现数据。
